diff --git a/src/main/resources/openapi.yml b/src/main/resources/openapi.yml
index 1234567..abcdefg 100644
--- a/src/main/resources/openapi.yml
+++ b/src/main/resources/openapi.yml
@@ -458,6 +458,11 @@ components:
           format: date
           description: The pet's birth date
           example: "2023-01-15"
+        weight:
+          type: number
+          format: double
+          nullable: true
+          description: The pet's weight in kg (decimal, optional)
         type:
           $ref: '#/components/schemas/PetType'
         owner:
@@ -491,6 +496,11 @@ components:
           format: date
           description: The pet's birth date
           example: "2023-01-15"
+        weight:
+          type: number
+          format: double
+          nullable: true
+          description: The pet's weight in kg (decimal, optional)
         typeId:
           type: integer
           format: int64
